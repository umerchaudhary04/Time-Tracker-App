name: Flutter CI (Dart)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        flutter-version: [stable, beta]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ matrix.flutter-version }}

      - name: Cache Pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            .dart_tool/pub
          key: ${{ runner.os }}-pub-${{ matrix.flutter-version }}-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pub-${{ matrix.flutter-version }}-

      - name: Cache Gradle
        if: matrix.os == 'ubuntu-latest'
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/gradle/wrapper/gradle-wrapper.properties') }}

      - name: Cache CocoaPods
        if: matrix.os == 'macos-latest'
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Caches/CocoaPods
            ios/Pods
          key: ${{ runner.os }}-cocoapods-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-cocoapods-

      - name: Install dependencies
        run: flutter pub get

      - name: Verify formatting
        run: flutter format --set-exit-if-changed .

      - name: Analyze project source
        run: flutter analyze

      - name: Run tests
        run: flutter test --no-fatal-infos

      - name: Build Android (APK)
        if: matrix.os == 'ubuntu-latest' || matrix.os == 'macos-latest'
        run: flutter build apk --release

      - name: Upload Android APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-apk-${{ matrix.flutter-version }}-${{ matrix.os }}
          path: build/app/outputs/flutter-apk/*.apk

      - name: Ensure CocoaPods is available
        if: matrix.os == 'macos-latest'
        run: |
          if ! command -v pod >/dev/null 2>&1; then
            sudo gem install cocoapods --no-document
          fi

      - name: Build iOS (no codesign)
        if: matrix.os == 'macos-latest'
        run: |
          flutter build ios --no-codesign --release

      - name: Upload iOS artifacts
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: ios-artifacts-${{ matrix.flutter-version }}
          path: |
            build/ios/ipa/**/*.ipa
            build/ios/iphoneos/**/*.app

      # Optional: collect logs or test reports here (e.g., JUnit-style test outputs), or add code coverage upload.
